# Run in a terminal opened in the folder, not within Visual Studio Code
> bundle install

# To launch in localhost type in terminal (on master branch):
> bundle exec jekyll serve

# Save changes before deployment (on master branch):
> git add + commit + push

# Deploy (on master branch, from gitbash, not windows powershell)
> bin/deploy --user