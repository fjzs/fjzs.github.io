# To launch in localhost type in terminal (on master branch):
> bundle exec jekyll serve (on master branch)

# Save changes before deployment (on master branch):
> git add + commit + push

# Deploy (on master branch, from gitbash)
> bin/deploy --user